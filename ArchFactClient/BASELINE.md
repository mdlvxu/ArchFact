# ArchFactClient 代码基线

- 基线日期：2026-07-28
- 基线分支：`main`
- 基线标签：`quality-baseline-v1`
- 配套后端：`ArchFactServer` 的同名标签

## 固定范围

该基线固定当前内容预览、PDF 长图导航、器物卡片联动、彩色横线与跨页范围框、
文本证据完整提取及相关质量评估界面的实现。

本地环境变量、依赖、构建结果、运行日志、工具密钥和增量编译缓存不属于代码基线。

## 验证结果

在基线提交前执行：

- `vitest run`：13 个测试文件通过，65 项测试通过，1 项测试跳过。
- `vue-tsc --noEmit`：通过。
- `eslint .`：0 个错误；保留 112 条既有模板排版警告，后续单独治理。

## 回退方式

先保存未提交的本地工作，再切换到 `quality-baseline-v1` 标签即可查看本基线。
该标签与后端同名标签共同构成一套可复现版本。
