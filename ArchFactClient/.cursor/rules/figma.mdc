---
description: 根据 Figma 设计实现 Vue 和 Element Plus 页面时使用
alwaysApply: false
---

当用户提供 Figma 文件或节点链接时：

1. 首先通过 Figma MCP 读取选中节点。
2. 获取布局、间距、颜色、字体、圆角、阴影、组件和变量信息。
3. 先检查项目中是否存在可复用组件。
4. 优先使用 Element Plus 组件，而不是从零实现基础控件。
5. 将重复设计值映射为 CSS Variables 或 SCSS Variables。
6. 不使用大量固定像素和绝对定位完成视觉复刻。
7. 页面必须考虑常见桌面宽度以及窄屏显示。
8. 图片必须保留合理的 aspect-ratio。
9. 表单控件需要 label、错误状态和键盘可访问性。
10. 实现后检查页面是否存在溢出、错位和不必要的重复代码。

不要直接照搬 Figma 自动生成的 HTML/CSS。
要根据当前 Vue 项目结构转化为可维护的组件。
从 Figma 生成的组件需添加中文注释，说明布局区块与交互逻辑。
