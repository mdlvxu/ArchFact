---
description: Vue 3 项目的全局架构和代码生成规则
alwaysApply: true
---

# 交流方式

- 与开发者使用**中文**交流（解释、说明、问答）
- 代码标识符（变量名、函数名、文件名）保持英文，遵循常规命名规范

# 代码注释

- 生成的代码必须为**每个功能模块**添加**中文注释**
- 注释说明「做什么、为什么」，帮助初级开发者理解
- 复杂逻辑分步骤注释；简单一行代码不必过度注释

# 技术栈

- 使用 Vue 3、Vite、TypeScript、Element Plus、Pinia、Vue Router 和 Axios。
- Vue 组件统一使用 `<script setup lang="ts">`。
- 不使用 Vue Options API。
- 不引入其他 UI 组件库，除非用户明确要求。
- 不随意增加依赖，增加依赖前先检查 package.json 中是否已有可用工具。
- 使用 pnpm，不生成 package-lock.json 或 yarn.lock。

# TypeScript

- 所有新增业务代码必须使用 TypeScript。
- 禁止随意使用 `any`，未知数据使用 `unknown` 并进行类型缩小。
- 类型导入使用 `import type`。
- 函数参数、返回结果和接口数据必须有明确类型。
- 不使用 `@ts-ignore`，除非说明具体原因。
- API 返回数据不能直接断言为目标类型，必要时使用 Zod 校验。

# Vue

- 优先使用 computed、watch、watchEffect 和 composable。
- 不在模板中编写复杂表达式。
- 页面业务逻辑应抽取到 composables、stores 或 services。
- 不直接修改 props。
- 组件事件必须使用类型化 defineEmits。
- 单个 Vue 组件超过约 300 行时，优先拆分子组件或 composable。

# 架构

- API 请求必须通过 `src/api/http.ts`。
- 业务接口放在 `src/api/modules`。
- 全局状态放在 Pinia，局部状态保留在组件。
- 公共组件放在 `src/components/base`。
- 业务组件放在 `src/components/business`。
- 页面组件放在 `src/views`。
- 不修改自动生成的 auto-imports.d.ts 和 components.d.ts。

# 修改流程

完成代码修改后：

1. 运行 `pnpm type-check`。
2. 运行 `pnpm lint:check`。
3. 存在测试时运行 `pnpm test:run`。
4. 修复错误后再结束任务。
